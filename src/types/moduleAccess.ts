
// Extract the module access types to a separate file for better organization
export interface ModuleAccess {
  dashboard: boolean;
  batches: boolean;
  eggCollection: boolean;
  feedManagement: boolean;
  vaccination: boolean;
  sales: boolean;
  customers: boolean;
  calendar: boolean;
  warehouse: boolean;
  reports: boolean;
  userManagement: boolean;
}
